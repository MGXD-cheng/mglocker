# MG Locker 使用说明书

MG Locker 是一款 Android 电视锁机应用：把电视锁定在一个全屏界面，防止小孩/客人随意退出，家长可通过手机浏览器或遥控器秘技远程解锁。

## 一、主要功能

- 全屏锁定：锁定后按 Home、返回、菜单、设置等键均无法退出
- 双层锁定：系统 Lock Task 硬锁 + 设备管理员（防卸载/强停）+ Home 自动弹回软锁
- 后台守护：App 被强停后自动恢复锁定（设备管理员禁强停 + 前台服务 + START_STICKY + 60 秒心跳闹钟四层保活）
- 开机自启：电视开机自动进入锁定状态
- 局域网控制台：手机浏览器访问即可锁定/解锁/查看状态
- 遥控器秘技解锁：上上下下左左右右（Konami Code）

## 二、快速上手

1. 安装并打开 MG Locker，按提示激活「设备管理器」权限
2. 锁定界面出现即表示锁定成功
3. 日常使用：
   - 锁定：手机浏览器打开控制台，点「立即锁定」
   - 解锁：手机浏览器点「解除锁定」，或遥控器按 上上下下左左右右

## 三、局域网控制台

- 地址：`http://<电视IP>:8080`
- 电视 IP 显示在锁定界面底部（如 192.168.1.100）
- 手机与电视需在同一 Wi-Fi
- 控制台功能：查看锁定状态、一键锁定、一键解锁

## 四、遥控器秘技解锁

- 在锁定界面依次按：上、上、下、下、左、左、右、右
- 屏幕弹出「🎮 秘技发动！已解锁」即解锁成功
- 秘技解锁后不会自动重新锁定，需手动锁定

## 五、版本历史

### v1.17（当前）
- 修复：控制台「点按钮没反应」（根因：/lock /unlock 返回无 Location 头的 301，页面 fetch() 直接失败）
- 优化：/lock /unlock 改为返回 200 JSON，控制台页面新增实时状态徽章（🔒已锁定 / 🔓已解锁）与 toast 操作反馈
- 说明：v1.16 为 v1.14 功能正式固化的 release 版（跳过 v1.15 中间测试版）

### v1.16
- 正式版：v1.14 心跳保活功能固化，构建 release 包（debug keystore 签名，覆盖安装保留设备管理员激活状态）
- 修复：AGP 9.0 release 资源优化在构建环境静默失败导致 APK 缺 manifest/arsc（禁用 android.enableResourceOptimizations）
- 验证：安装后未打开 App，70 秒后心跳自动拉起进程并锁定（真实场景保活成功）

### v1.15
- 测试版：尝试自杀测试广播验证心跳（exported=false 广播 shell 身份投递不进），功能并入 v1.16

### v1.14
- 新增：HeartbeatReceiver 60 秒心跳闹钟（AlarmManager 循环续期），进程被杀后由系统强制复活并恢复锁定
- 完善：四层防杀体系（设备管理员禁强停 → 前台服务 → START_STICKY → 60s 心跳）

### v1.13
- 修复：Home 后打开其他软件无法锁回（根因：Activity 后台 startActivity 被 TCL ROM 拦截，之前依赖 Activity 自杀式弹回）
- 重构：Home 弹回改为 GuardService 前台服务 500ms 轮询拉起，无限重试，任何情况都能锁回
- 新增：isForeground / navigatingAway 静态状态标志，服务据此判断是否拉起（解锁/跳设置页自动停手）
- 声明悬浮窗权限（部分 ROM 对持此权限的应用放行后台启动）

### v1.12
- 修复：Home 后快速打开其他 App 时弹回失效（去掉防抖挡板，onUserLeaveHint/onStop 双枪齐发）
- 补充：此版本未解决 TCL 后台启动拦截问题，由 v1.13 的轮询方案根治

### v1.11
- 修复：远程解锁偶发失效（多实例残留 + LockTask 硬锁未解除导致"解不开"）
- 修复：跳转系统授权/设置页时锁屏界面抢回前台，盖住设置页无法操作
- 新增：控制台解锁确认弹窗（页面内弹窗，防误触）
- 优化：解锁后残留实例回到前台自动退出，保证彻底解锁
- 优化：解锁时主动解除 Lock Task 硬锁

### v1.10
- 修复：控制台解锁在特定时序下失效（锁定启动延迟期间点解锁会被覆盖）
- 新增：遥控器秘技解锁（上上下下左左右右）
- 优化：控制台解锁去掉确认弹窗，响应更直接
- 优化：状态接口版本号动态读取，不再写死

### v1.9
- 修复：解锁后无法上锁（假锁定：界面出现但按 Home 可退出）
- 修复：解锁彻底清空任务栈，防止残留实例
- 加固：锁定接口与 Activity 复用场景强制复位锁定状态

### v1.8
- 新增：局域网控制台网页版（状态显示 + 锁定/解锁按钮）
- 加固：设备管理员防卸载/强停 + Home 自动弹回软锁
- 修复：App 被强停后自动恢复锁定

### v1.7 及更早
- 核心功能建立：全屏锁定、双层锁定、后台守护服务、开机自启、HTTP 控制接口（/lock /unlock /status）
